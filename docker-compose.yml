services:
  hangke-ai-edu:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    volumes:
      # Optional: mount server-providers.yml for provider config
      # - ./server-providers.yml:/app/server-providers.yml:ro
      - hangke-ai-edu-data:/app/data
    restart: unless-stopped

volumes:
  hangke-ai-edu-data:
